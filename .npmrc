# The langchain ecosystem ships overlapping optional peer ranges that npm's strict
# resolver rejects. legacy-peer-deps relaxes that (the original repo used pnpm, which
# is laxer here). Safe for this project.
legacy-peer-deps=true
